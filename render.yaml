services:
  - type: web
    name: bulk-whatsapp
    runtime: node
    plan: free
    buildCommand: ./build.sh
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
